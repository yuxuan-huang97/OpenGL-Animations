// This is a helper function to export window content in ASCII PPM format
// The original code was part of the 1D bouncing ball example for CSCI 5611
// and is separated into this standalone file for more flexible use

#include <fstream>
#include "../../glad/glad.h" 

void Win2PPM(int width, int height);